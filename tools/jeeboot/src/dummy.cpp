// We need to have at least one source file, and it needs to contain some
// actual code so that the cpputest build system doesn't complain.

void dummy ();      // prototype
void dummy () {}    // implementation
